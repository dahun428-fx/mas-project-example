# 실제 키는 .env 에만 넣는다. 이 파일은 git 에 올라가므로 값은 항상 비워 둔다.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=            # 지금은 안 씀

# 역할별 모델 (미설정 시 mini_mas/config.py 의 기본값). 개발 중엔 nano/mini 급만.
# KNOWLEDGE_MODEL=gpt-5.4-nano
# CLASSIFIER_MODEL=gpt-5.4-nano       # Step 3
# SYNTHESIZER_MODEL=gpt-5.4-nano      # Step 4
